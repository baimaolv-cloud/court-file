---
name: court-file
description: 诉讼文书制作技能。适用于民事起诉状、答辩状、上诉状等诉讼文书的起草、审定与定稿。触发词：起诉状、答辩状、上诉状、诉讼文书、民事诉状、法院文书、立案、诉状审定、证据整理、法条核对。覆盖从信息采集→结构搭建→逐段撰写→证据编号管理→金额核算→法条核对→策略决策→多遍审定→导出定稿→网上立案的完整工作流。
---

# 诉讼文书制作

## 工作流概览

```
信息采集 → 结构搭建 → 逐段撰写 → 证据编号管理 → 金额核算 → 法条核对 → 策略决策 → 多遍审定 → 导出定稿 → 网上立案
```

**详细操作规范**见 [references/workflow.md](references/workflow.md)
**避坑指南**见 [references/pitfalls.md](references/pitfalls.md)
**策略决策框架**见 [references/strategy.md](references/strategy.md)
**网上立案指南**见 [references/online-filing.md](references/online-filing.md)

## 快速开始

### 1. 信息采集

收集当事人信息（姓名/信用代码/住址/法定代表人）、事实材料（时间线+合同+支付凭证+沟通记录+损害证据+行政维权记录）。

OCR工具链选择：
- PDF有文本层 → PyMuPDF直接提取
- PDF扫描件 → `sips`转PNG → Swift Vision OCR
- 红/金底色证书 → **不OCR**，按文件名列出
- 手写体 → 不可靠，跳过

### 2. 撰写

按标准结构撰写：原告信息→被告信息→案由→诉讼请求→事实与理由→证据清单→管辖依据→此致→签名→日期。

多案由合并时，按主次排列，最后设"案由关联性"部分论证合并审理依据。

### 3. 证据编号管理（最易出错）

**核心规则：**
- 连续整数编号，禁止子编号（不用78-1、80-2）
- 新增证据追加末尾，不在中间插入
- 格式统一：正文引用与清单格式一致
- 批量重编号用Python脚本，从大到小替换，替换后全文验证
- **维护证据↔论证交叉引用表**（见workflow.md附录）

### 4. 金额核算

**单一数据源原则**：每个金额只在一处定义。修改时全文grep搜索所有涉及位置同步更新。

**必验算**：诉讼请求总额 = 各案由金额之和 = 各分项之和。标的额保留到小数点后一位。

### 5. 法条核对

法条用语**必须**对照最新原文，一个字都不能差。特别注意修法后的编号变化和术语差异（如"举证责任转移"非"倒置"）。

### 6. 策略决策

每个索赔/论证项必须明确归类（见[策略决策框架](references/strategy.md)）：
- **索赔项**：写入诉讼请求，有金额
- **论证项**：写入事实与理由，无金额但支撑索赔
- **保留项**：本案不主张，保留另行主张权利
- **剔除项**：不写入，记录决策理由备查

**决策一经做出，记入决策日志，不再重新讨论。**

### 7. 五遍审定

| 遍数 | 重点 | 方法 |
|------|------|------|
| 1 | 逻辑一致性 | 前后论述是否矛盾、事实与诉请是否对应 |
| 2 | 金额 | 分项加总、汇总一致、清单与正文一致 |
| 3 | 法条 | 编号正确、术语精确、生效日期 |
| 4 | 证据链 | 编号连续、格式统一、证据与论证对应 |
| 5 | 措辞 | 术语统一、错别字、标点、格式 |

审定前先运行自动验证脚本：
```bash
python3 scripts/validate.py <input.md>
```

### 8. 导出定稿

确保md定稿后执行导出，三文件时间戳一致。

**Word导出：**
```bash
node scripts/md2docx.js <input.md> [output.docx]
```

**PDF导出：**
```bash
python3 scripts/md2pdf.py <input.md> [output.pdf]
```

**⚠️ 导出后必检**：
- docx打开确认无乱码、格式正常
- pdf打开确认中文正常、无markdown残留（如`**`）
- 三文件（md/docx/pdf）版本号一致

### 9. 证据文件打包

将证据文件按编号重命名打包，用于立案提交。

**扫描建索引：**
```bash
python3 scripts/evidence_pack.py --index /path1 /path2 --index-output /tmp/index.json --mapping /tmp/mapping.json
```

**按映射打包：**
```bash
python3 scripts/evidence_pack.py --mapping /tmp/mapping.json --output /tmp/证据打包 --zip /output/证据材料.zip
```

**⚠️ 映射必须人工确认**：自动匹配不可靠，最终映射必须逐项人工校验。详见[工作流](references/workflow.md)和[避坑指南](references/pitfalls.md)。

## 关键避坑速查

| 雷区 | 规则 |
|------|------|
| docx XML编辑 | 能不做就不做，优先回md重编再导出；必须做时先审计段落结构、建映射表、一次性替换 |
| 证据编号 | 连续整数，禁子编号，批量替换用Python从大到小 |
| 证据打包 | 人工精确映射，自动匹配只辅助；先全盘扫描建索引；缺失项分三类标注原因 |
| 金额 | 单一数据源，改一处grep全文同步，必独立验算总额 |
| 法条 | 对照原文，术语一个字不能差，确认生效日期 |
| 主体 | 首次全称+编号，后续统一编号，账号/合同/付款方对号入座 |
| 策略 | 决策即记录，已决不再议，保留项标注"另行主张" |
| 编辑 | 每次只替一个不重叠块，oldText必须唯一，替换后立即验证 |
| OCR | 红金底/手写体不OCR，结果必校验关键信息 |
| 版本 | 文件名与内容版本一致，导出后核对时间戳 |
